# https://developers.home-assistant.io/docs/add-ons/configuration#add-on-config
# Edge build: version is auto-bumped to the short git SHA by the CI workflow
name: Logspout (edge)
version: "6f648c7"
image: "ghcr.io/bertbaron/{arch}-addon-logspout"
slug: logspout
description: "Send HA logging to remote log management systems"
url: "https://github.com/bertbaron/hassio-addons/tree/main/logspout"
arch:
  - aarch64
  - amd64
init: false
startup: system
advanced: true
docker_api: true

map:
  - config

options:
  routes:
    - gelf://graylog.home:12201
  env: []
schema:
  routes:
    - str?
  hostname: str?
  strip_ansi: bool?
  env:
    - name: str
      value: str
